'use babel';

import SpellCheckLanguagetoolView from '../lib/spell-check-languagetool-view';

describe('SpellCheckLanguagetoolView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
